const baseURL = 'http://localhost:3002'

const logIn = '/auth/sign-in'
const users = '/users'
const orders = '/orders'
const groups = '/groups'

const urls = {
    logIn,
    orders,
    users,
    groups
}
export {
    baseURL,
    urls
}